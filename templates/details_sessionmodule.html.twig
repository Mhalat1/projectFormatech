<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <title>Détails de la Session et du Module</title>
</head>
<body class="bg-gray-50 p-6 font-sans">
    <div class="container mx-auto">
        <!-- Titre principal -->
        <h1 class="text-4xl font-extrabold text-center text-gray-900 mb-12">Détails de la Session et du Module</h1>

        {% if sessions is not empty %}
            <div class="bg-white shadow-xl rounded-lg p-6">
                <!-- Affichage des détails des sessions et des modules -->
                {% for session in sessions %}
                    <div class="space-y-6">
                        <h2 class="text-2xl font-semibold text-gray-800">Session : {{ session.sessionNom }}</h2>
                        
                        <div class="mt-8 bg-blue-100 p-6 rounded-xl">
                            <h3 class="text-xl font-semibold text-blue-600">Module Associé : {{ session.moduleNom }}</h3>
                            <p><strong>ID du Module :</strong> {{ session.moduleId }}</p>
                            <p><strong>Description du Module :</strong> {{ session.moduleDescription }}</p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <p class="text-red-500 text-center text-lg mt-6">Aucune donnée trouvée pour cette session.</p>
        {% endif %}
        
        <!-- Bouton retour -->
        <div class="mt-8 text-center">
            <a href="{{ path('app_Session_Module') }}" class="inline-block bg-blue-500 text-white px-6 py-3 rounded-lg shadow-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-400 transition duration-200">
                Retour à la liste des modules et sessions
            </a>
        </div>
    </div>
</body>
</html>
